let arr = [1, 2, 3, 4, 5];
let arr2 = [100, 101, 102];
let totalarr = 0;
let totalarr2 = 0;

for (let i in arr) {
    totalarr += arr[i];
    avgarr = totalarr / arr.length }
    console.log(avgarr);

for (let i in arr2) {
    totalarr2 += arr2[i];
    avgarr2 = totalarr2 / arr2.length}
    console.log(avgarr2);